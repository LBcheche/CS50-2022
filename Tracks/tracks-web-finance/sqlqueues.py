import os
from cs50 import SQL


def searchAllFromWalletWith(user_id, share_id):
    #db.execute("SELECT * FROM wallet WHERE user_id = :uid AND share_id = :sid", uid = user_id, sid = share_id)
    return
